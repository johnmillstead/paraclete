---
import { getCollection } from "astro:content";
const staff = await getCollection("staff");
---

<h1>People</h1>
<p>Staff count: {staff.length}</p>

<ul>
  {staff.map((entry) => (
    <li>{entry.data.name} — {entry.data.title}</li>
  ))}
</ul>